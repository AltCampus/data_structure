// --- Description
// create a Node class with data and priority properties
// Create a priority_queue data structure.  The priority_queue
// should be a class with methods 'add' and 'remove'.
// Adding to the queue should store an element until
// it is removed
// Each element added should have a priority(from 1 to 5) or defaults to zero.
// --- Examples
//     const q = new PriorityQueue();
//     q.add('xyz', 3);
//     q.add('abc', 5);
//     q.add('rst', 2);
//     q.remove(); // returns 'abc';

class Node {
  
}

class PriorityQueue {
  
}

module.exports = {PriorityQueue, Node};
